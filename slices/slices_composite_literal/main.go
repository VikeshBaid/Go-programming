package main

import (
	"fmt"
)

func main() {
	//COMPOSITE LITERALS
	// means declaring array, slice, struct, map using type declaration and then giving value in curly braces
	x := []int{1, 2, 4, 53}
	fmt.Println(x)
}
