package main

import "fmt"

func main() {
	n, _ := fmt.Println("hello")
	fmt.Println(n) // n is a return value coming from the return value of Println. Give number of bytes in integer.
	//fmt.Println(err) // underscoer is used to throw away the return value (err) from the funciton. It returns the error if any during writing process.
}
